// Importing a custom module in the directory
// './' represents the current directory
const things = require('./things');

// importing the events core module
// does not require './'
const events = require('events');
